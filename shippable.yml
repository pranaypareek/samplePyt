# Language setting
language: python

# Version number - master
python:
  - 2.6

build:
  ci:
    - python --version

integrations:
  -notifications
    - integrationName: irc
      type: irc
      recipients:
      - "chat.freenode.net#jatin"
      on_success: change
      on_failure: always
      on_start: never
      on_pull_request: always
